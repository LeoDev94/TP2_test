const database = {
    HOST: 'localhost',
    USER: 'root', // Change credentials
    PASSWORD: 'root', // Change credentials
    DATABASE: "collaborator_db",
    TIMEOUT: 20000
}

module.exports = {
    database
}